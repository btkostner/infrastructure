ingress:
  enabled: false

parent:
  envFrom:
    - secretRef:
        name: netdata-credentials
  database:
    persistence: false
  alarms:
    persistence: false
  claiming:
    enabled: true

child:
  envFrom:
    - secretRef:
        name: netdata-credentials
  persistence:
    enabled: false
  claiming:
    enabled: true

k8sState:
  envFrom:
    - secretRef:
        name: netdata-credentials
  persistence:
    enabled: false
  claiming:
    enabled: true
