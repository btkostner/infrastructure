apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: longhorn-http
  namespace: longhorn-system
spec:
  hostnames:
    - longhorn.btkostner.network
  parentRefs:
    - name: internal-gateway
      namespace: networking
  rules:
    - backendRefs:
        - name: longhorn-frontend
          port: 80
