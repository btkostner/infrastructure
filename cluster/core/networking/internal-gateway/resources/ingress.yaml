apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: internal-gateway
  namespace: networking
spec:
  defaultBackend:
    service:
      name: cilium-gateway-internal-gateway.networking.svc.cluster.local
      port:
        number: 443
  ingressClassName: tailscale
