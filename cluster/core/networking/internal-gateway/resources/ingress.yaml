apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: internal-ingress
  namespace: networking
spec:
  defaultBackend:
    service:
      name: internal-gateway
      port:
        number: 443
  ingressClassName: tailscale
  tls:
    - hosts:
        - "*.btkostner.network"
      secretName: btkostner-network
