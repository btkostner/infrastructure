apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: autoscan-media
  namespace: media
spec:
  storageClassName: nfs
  accessModes:
    - ReadWriteMany
  volumeName: media-autoscan-media
  resources:
    requests:
      storage: 1Mi
