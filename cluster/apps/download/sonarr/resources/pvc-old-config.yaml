apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sonarr-old-config
  namespace: download
spec:
  storageClassName: synology-iscsi
  accessModes:
    - ReadWriteOnce
  volumeName: download-sonarr-old-config
  resources:
    requests:
      storage: 10Gi
