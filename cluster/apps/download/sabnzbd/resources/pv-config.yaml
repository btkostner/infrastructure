apiVersion: v1
kind: PersistentVolume
metadata:
  name: download-sabnzbd-config
spec:
  accessModes:
    - ReadWriteOnce
  capacity:
    storage: 10Gi
  claimRef:
    name: sabnzbd-config
    namespace: download
  csi:
    driver: driver.longhorn.io
    volumeHandle: download-sabnzbd-config
    fsType: ext4
  persistentVolumeReclaimPolicy: Delete
  storageClassName: longhorn
  volumeMode: Filesystem
