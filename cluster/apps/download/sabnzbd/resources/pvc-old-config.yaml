apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sabnzbd-old-config
  namespace: download
spec:
  storageClassName: synology-iscsi
  accessModes:
    - ReadWriteOnce
  volumeName: download-sabnzbd-old-config
  resources:
    requests:
      storage: 20Gi
