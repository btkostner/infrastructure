apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: seerr-config
  namespace: download
spec:
  volumeMode: Filesystem
  storageClassName: ceph-block-replicated
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
