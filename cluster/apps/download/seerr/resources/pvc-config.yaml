apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: seerr-config
  namespace: download
spec:
  volumeMode: Filesystem
  storageClassName: ceph-safe-block
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
