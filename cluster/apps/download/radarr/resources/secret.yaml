apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: radarr-config
  namespace: download
  annotations:
    operator.1password.io/auto-restart: true
spec:
  itemPath: vaults/Infrastructure/items/Radarr Config
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
