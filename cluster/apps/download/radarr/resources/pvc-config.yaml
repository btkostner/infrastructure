apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: radarr-config
  namespace: download
spec:
  volumeMode: Filesystem
  storageClassName: longhorn
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
