apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: radarr-old-config
  namespace: download
spec:
  storageClassName: synology-iscsi
  accessModes:
    - ReadWriteOnce
  volumeName: download-radarr-old-config
  resources:
    requests:
      storage: 10Gi
