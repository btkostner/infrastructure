config:
  executions:
    dataMaxAge: 3760
    dataPrune: true
    pruneData: null
    pruneDataMaxAge: null

image:
  pullPolicy: Always
  tag: 1.68.0

replicaCount: 1

persistence:
  enabled: true
  type: existing
  existingClaim: n8n-config
