config:
  executions: null
  pruning:
    isEnabled: true
    maxAge: 3760
    maxCount: 10_000

image:
  pullPolicy: Always
  tag: 1.68.0

replicaCount: 1

persistence:
  enabled: true
  type: existing
  existingClaim: n8n-config
