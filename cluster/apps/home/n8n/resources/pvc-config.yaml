apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: n8n-config
  namespace: home
spec:
  volumeMode: Filesystem
  storageClassName: ceph-block-replicated
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
