apiVersion: v1
kind: PersistentVolume
metadata:
  name: home-homebridge-config
spec:
  storageClassName: rook-ceph-block
  persistentVolumeReclaimPolicy: Retain
  accessModes:
    - ReadWriteOnce
  capacity:
    storage: 25Gi
