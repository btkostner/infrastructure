apiVersion: v1
kind: PersistentVolume
metadata:
  name: home-homebridge-config
spec:
  storageClassName: rook-ceph-block
  accessModes:
    - ReadWriteMany
  capacity:
    storage: 25Gi
  volumeMode: Block
