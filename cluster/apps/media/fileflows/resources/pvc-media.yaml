apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: fileflows-media
  namespace: media
spec:
  storageClassName: nfs
  accessModes:
    - ReadWriteMany
  volumeName: media-fileflows-media
  resources:
    requests:
      storage: 1Mi
