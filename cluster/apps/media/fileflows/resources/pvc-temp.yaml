apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: fileflows-temp
  namespace: media
spec:
  storageClassName: nfs
  accessModes:
    - ReadWriteMany
  volumeName: media-fileflows-temp
  resources:
    requests:
      storage: 1Mi
