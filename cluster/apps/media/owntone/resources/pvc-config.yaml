apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: owntone-config
  namespace: media
spec:
  volumeMode: Filesystem
  storageClassName: longhorn
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
