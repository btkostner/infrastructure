apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: owntone-media
  namespace: media
spec:
  storageClassName: nfs
  accessModes:
    - ReadWriteMany
  volumeName: media-owntone-media
  resources:
    requests:
      storage: 1Mi
