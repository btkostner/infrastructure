apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dizquetv-media
  namespace: media
spec:
  storageClassName: nfs
  accessModes:
    - ReadWriteMany
  volumeName: media-dizquetv-media
  resources:
    requests:
      storage: 1Mi
