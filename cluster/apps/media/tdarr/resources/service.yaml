apiVersion: v1
kind: Service
metadata:
  name: tdarr
  namespace: media
spec:
  selector:
    app: tdarr-server
  ports:
    - name: server
      port: 8266
      protocol: TCP
      targetPort: 8266
    - name: dashboard
      port: 80
      protocol: TCP
      targetPort: 8265
