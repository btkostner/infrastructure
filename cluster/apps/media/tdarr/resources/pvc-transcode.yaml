apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tdarr-transcode
  namespace: media
spec:
  storageClassName: nfs
  accessModes:
    - ReadWriteMany
  volumeName: media-tdarr-transcode
  resources:
    requests:
      storage: 1Mi
