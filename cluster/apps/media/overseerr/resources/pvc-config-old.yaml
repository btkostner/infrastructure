apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: overseerr-config-old
  namespace: media
spec:
  storageClassName: synology-iscsi
  accessModes:
    - ReadWriteOnce
  volumeName: media-overseerr-config
  resources:
    requests:
      storage: 10Gi
