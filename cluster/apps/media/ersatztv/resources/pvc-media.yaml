apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ersatztv-media
  namespace: media
spec:
  storageClassName: nfs
  accessModes:
    - ReadWriteMany
  volumeName: media-ersatztv-media
  resources:
    requests:
      storage: 1Mi
