---

- name: Enable "containerd"
  service:
    name: docker
    enabled: true
  become: true

- name: Restart "containerd"
  service:
    name: docker
    state: restarted
  become: true

- name: Restart server
  reboot:
    connect_timeout: 5
    reboot_timeout: 600
  become: true
