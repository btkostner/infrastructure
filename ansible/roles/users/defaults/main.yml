---

users_sudo_group: admin
users_default_shell: /bin/bash

# Always put the root user last, to make sure we can setup users before changing
# the possible provision user key
users:
  - uid: 0
    name: Gabriel Grey
    username: root
    password: "!"
    shell: /bin/bash
    ssh_keys: []
